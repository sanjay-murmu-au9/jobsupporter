name: Deploy to EC2

on:
  push:
    branches: [ dev ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build

      - name: Create deployment package
        run: |
          zip -r deploy.zip . -x "node_modules/*" ".git/*" "dist/*"

      - name: Test SSH Connection
        run: |
          echo "Testing SSH connection..."
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          echo "SSH key permissions set to 600"
          
          HOST="${{ secrets.EC2_HOST }}"
          echo "Testing connection to $HOST..."
          ssh -o StrictHostKeyChecking=no -v ubuntu@$HOST 'echo "SSH Connection Successful"'

      - name: Deploy to EC2
        if: success()
        run: |
          HOST="${{ secrets.EC2_HOST }}"
          echo "Deploying to EC2 at $HOST..."
          scp -o StrictHostKeyChecking=no -v deploy.zip ubuntu@$HOST:~/jobsupport/
          
          echo "Running deployment commands..."
          ssh -o StrictHostKeyChecking=no ubuntu@$HOST '
            cd ~/jobsupport && \
            unzip -o deploy.zip && \
            npm ci --production && \
            pm2 restart ecosystem.config.js || pm2 start ecosystem.config.js && \
            rm deploy.zip && \
            echo "Deployment completed successfully!"
          ' 